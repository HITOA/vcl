#ifndef SOURCE_ERROR
    #define SOURCE_ERROR(x, y)
#endif
#ifndef LEXER_ERROR
    #define LEXER_ERROR(x, y)
#endif

SOURCE_ERROR(FileNotFound,                  "File not found")
SOURCE_ERROR(MemoryBufferCreationFailed,    "Memory buffer creation failed")
SOURCE_ERROR(InvalidSourceID,               "Invalid source id")

LEXER_ERROR(InvalidCharacter,               "Invalid character")
LEXER_ERROR(UnterminatedString,             "Unterminated string")
LEXER_ERROR(NumericConstantTooMuchText,     "Too much text after expected end of numeric constant")

#undef SOURCE_ERROR
#undef LEXER_ERROR